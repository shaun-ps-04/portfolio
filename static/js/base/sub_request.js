const sub_btn = $(".sub_form__submit_container > input");
const user_email = $(".sub_form__email_container > input");

console.log(sub_btn);

sub_btn.on('click', e => {

})

// Upon subscribe click, trigger subscribe 

// Send ajax request with email

// If success close subscribe form


